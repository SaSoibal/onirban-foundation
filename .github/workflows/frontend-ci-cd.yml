name: Frontend CI/CD

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - ".github/workflows/frontend-ci-cd.yml"

  pull_request:
    branches: [main]
    paths:
      - "frontend/**"

  workflow_dispatch:

# Allow GitHub Actions to push to gh-pages
permissions:
  contents: write

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

      - name: Run tests
        run: npm test -- --watch=false

  deploy:
    needs: lint-and-test
    runs-on: ubuntu-latest

    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Add .nojekyll to bypass Jekyll processing
        run: touch frontend/dist/.nojekyll

      - name: Verify build output
        run: ls -la frontend/dist && cat frontend/dist/index.html | head -5

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: frontend/dist
          publish_branch: gh-pages
          force_orphan: true
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"

      - name: Verify GitHub Pages deployment
        run: |
          echo "Frontend deployed to: https://sasoibal.github.io/onirban-foundation/"
          echo "If you see README instead of the app:"
          echo "1. Go to Settings → Pages"
          echo "2. Set Source to 'gh-pages' branch, / (root) folder"
          echo "3. Save and wait 1-2 minutes"
